---
description: Append a durable lesson to docs/02 Code/Engineering Lessons.md.
---

Append a Worldbuilder-specific lesson to `docs/02 Code/Engineering Lessons.md` in the format: **rule** (one line) + why + `[Verified: YYYY-MM-DD]`. Put it under the right section (add one if needed).

If the lesson is genuinely cross-project (true for ANY project), put it in the global layer instead: capture in `~/.claude/PROMOTION-INBOX.md`, which drains into `~/.claude/Engineering Lessons.md`.
