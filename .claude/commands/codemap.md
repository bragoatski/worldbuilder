---
description: Refresh docs/02 Code/CodeMap.md against the current index.html.
---

Re-verify and refresh the navigation index in `docs/02 Code/CodeMap.md` against the current `index.html`.

1. Confirm the section order and key symbols still match the file (grep for each named function).
2. Fill in / correct line numbers for each section and symbol (left as PENDING at bootstrap).
3. Add any new systems; mark removed ones.

Keep it terse - it is a navigation aid, not documentation.
